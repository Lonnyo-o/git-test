# JavaScript 常用字符串方法笔记
> 📌 核心前提：所有字符串方法**都不会修改原字符串**，只会返回新的结果，使用时需要用变量接收返回值。

---

## 一、字符串截取类
### 1. substr()：按「起始位置 + 长度」截取
- **语法**：`原字符串.substr(起始下标, 截取长度)`
- **大白话**：从指定下标开始，往后截取指定个数的字符。
- **参数说明**：
  - 起始下标（必填）：开始截取的索引位置
  - 截取长度（可选）：要截取的字符数量；不写则直接截取到字符串末尾
  -  **start（第一个参数）**：可以传负数 负数代表**从字符串末尾倒数**计算起始位置 
- **代码示例**
```javascript
var str = "abcdefg"; // 索引对应：0=a,1=b,2=c,3=d,4=e,5=f,6=g
console.log(str.substr(2, 3));  // 从索引2开始，取3个 → cde
console.log(str.substr(3, 6));  // 从索引3开始，长度不足6个，取到末尾 → defg
console.log(str.substr(0, 6));  // 从索引0开始，取6个 → abcdef（原注释错误，已修正）
console.log(str.substr(2));     // 不写长度，从索引2取到末尾 → cdefg
```
- **注意**：起始下标支持负数（从末尾倒数），长度参数不能为负。

### 2. substring()：按「起止索引」截取
- **语法**：`原字符串.substring(开始下标, 结束下标)`
- **大白话**：截取两个索引之间的字符，遵循**包前不包后**规则（包含起始位置，不包含结束位置）。
- **参数说明**：
  - 开始下标（必填）：开始截取的索引位置
  - 结束下标（可选，原注释“必填”表述错误，已修正）：结束位置；不写则截取到字符串末尾
- **代码示例**
```javascript
var str = "abcdefg";
console.log(str.substring(2, 5));  // 截取索引2~5之前 → cde
console.log(str.substring(3, 6));  // 截取索引3~6之前 → def
console.log(str.substring(0, 6));  // 截取索引0~6之前 → abcdef
console.log(str.substring(2));     // 不写结束位，取到末尾 → cdefg
```
- **注意**：不支持负数参数，传入负数会自动当成 0 处理。

### 3. slice()：按「起止索引」截取（日常推荐）
- **语法**：`原字符串.slice(起始索引, 结束索引)`
- **大白话**：和 substring 规则一致（包前不包后），额外支持负数索引（从字符串末尾倒数），功能最全，日常开发优先使用。
- **参数说明**：
  - 起始索引（必填）：开始截取的位置
  - 结束索引（可选）：结束位置；不写则截取到字符串末尾
- **代码示例**
```javascript
var num = "abc123456789999"; // 索引0~13，共14个字符
console.log(num.slice(3, 7));    // 截取索引3~7之前 → 1234
console.log(num.slice(-4, -1));  // 从倒数第4个截取到倒数第1个之前 → 999
console.log(num.slice(3));       // 从索引3取到末尾 → 123456789999
console.log(num.slice(-4));      // 截取最后4个字符 → 9999
```

---

## 二、查找位置类
### 1. indexOf()：查找首次出现的位置
- **语法**：`原字符串.indexOf(查找内容, 起始查找位置)`
- **大白话**：从左往右找，返回目标内容**第一次出现**的起始索引；找不到固定返回 `-1`。
- **参数说明**：
  - 查找内容（必填）：要查找的子字符串
  - 起始查找位置（可选）：从指定索引开始往后找，默认从0开始
- **代码示例**
```javascript
var num = "abc123456789999";
console.log(num.indexOf("ab"));  // 首次出现位置 → 0
console.log(num.indexOf("bc"));  // 首次出现位置 → 1
console.log(num.indexOf("1"));   // 首次出现位置 → 3
console.log(num.indexOf("x"));   // 找不到 → -1
```

### 2. lastIndexOf()：查找最后一次出现的位置
- **语法**：`原字符串.lastIndexOf(查找内容, 起始查找位置)`
- **大白话**：从右往左反向找，返回目标内容**最后一次出现**的起始索引；找不到固定返回 `-1`。
- **参数说明**：
  - 查找内容（必填）：要查找的子字符串
  - 起始查找位置（可选）：从指定索引开始往前找，默认从最后一位开始
- **代码示例**
```javascript
var num = "abc123456789999";
console.log(num.lastIndexOf("a"));  // 最后一次出现位置 → 0
console.log(num.lastIndexOf("b"));  // 最后一次出现位置 → 1
console.log(num.lastIndexOf("c"));  // 最后一次出现位置 → 2
console.log(num.lastIndexOf("9"));  // 最后一次出现位置 → 13（原注释12错误，已修正）
```

### 3. charAt()：根据索引取单个字符
- **语法**：`原字符串.charAt(下标)`
- **大白话**：返回指定索引位置对应的单个字符。
- **代码示例**
```javascript
var num = "abc123456789999";
var str = "abcdefg";
console.log(num.charAt(1));  // 索引1对应的字符 → b
console.log(str.charAt(2));  // 索引2对应的字符 → c
```
- **补充简便写法**：也可以直接用 `字符串[索引]` 获取，效果相同
```javascript
console.log(num[1]); // 等价于 num.charAt(1) → b
```

---

## 三、字符串替换
### replace()：替换匹配内容
- **语法**：`原字符串.replace(查找内容, 替换内容)`
- **大白话**：把匹配到的内容替换成新内容；**默认只替换第一个匹配项**。
- **代码示例**
```javascript
var num = "abc123456789999";
console.log(num.replace("1234", "替换成功"));  // abc替换成功56789999（原注释少1个9，已修正）
console.log(num.replace("999", "替换成功"));   // abc12345678替换成功（只替换第一个999）
console.log(num.replace("9999", "四个大字"));  // abc12345678四个大字
```
- **注意**：
  1. 默认只替换第一个匹配项；要替换所有内容，需用正则加 `g` 标志，或使用 `replaceAll()` 方法。
  2. 全局替换示例：`num.replace(/9/g, "x")` 会把所有9替换成x。

---

## 四、拼接与拆分
### 1. concat()：拼接字符串
- **语法**：`原字符串.concat(字符串1, 字符串2...)`
- **大白话**：把多个字符串依次拼接到原字符串后面，返回拼接后的新字符串，和 `+` 号拼接效果一致。
- **代码示例**
```javascript
var num = "abc123456789999";
var str = "abcdefg";
console.log(num.concat("我是你爸爸"));  // abc123456789999我是你爸爸
console.log(str.concat("你好"));       // abcdefg你好
```

### 2. split()：字符串切割为数组
- **语法**：`原字符串.split(分隔符)`
- **大白话**：用指定符号当“剪刀”，把字符串拆分成数组；分隔符本身会被移除。
- **代码示例**
```javascript
var num = "abc123456789999";
// 传空字符串：每个字符单独拆成数组项
console.log(num.split(""));  
// 结果：['a','b','c','1','2','3','4','5','6','7','8','9','9','9','9']

// 补充：按指定符号拆分
var fruit = "苹果,香蕉,橘子";
console.log(fruit.split(",")); // ['苹果', '香蕉', '橘子']
```

---

## 五、大小写转换
> 两个方法均无需传参，括号留空即可。
### 1. toUpperCase()：转为全大写
- **大白话**：把字符串里所有字母全部变成大写。
- **代码示例**
```javascript
var num = "abc123456789999";
var str = "abcdefg";
console.log(num.toUpperCase());  // ABC123456789999
console.log(str.toUpperCase());  // ABCDEFG
```

### 2. toLowerCase()：转为全小写
- **大白话**：把字符串里所有字母全部变成小写。
- **代码示例**
```javascript
var num = "abc123456789999";
console.log(num.toLowerCase());  // abc123456789999
```

---

## 补充：常用属性
### length：获取字符串长度
- **说明**：它是属性不是方法，**不加括号**，返回字符串的字符总数。
```javascript
var str = "abcdefg";
console.log(str.length); // 7
```

---

## 易混截取方法对比表
| 方法 | 参数形式 | 支持负数索引 | 核心特点 |
|------|----------|--------------|----------|
| `slice()` | (起始索引, 结束索引) | ✅ 支持 | 功能最全，包前不包后，日常优先使用 |
| `substring()` | (开始下标, 结束下标) | ❌ 不支持（负数按0处理） | 包前不包后，仅支持正数 |
| `substr()` | (起始下标, 截取长度) | ✅️起始位支持，❌️长度不支持 | 按长度截取，已不推荐使用 |
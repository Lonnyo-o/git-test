# JavaScript 数组常用方法完整笔记
> 已修正原代码中的易错点，补充核心特性与注意事项，所有方法均标注「是否修改原数组」（高频考点）

---

## 一、数组的创建
### 1. 构造函数方式 `new Array()`    *`/*əˈreɪ*/*`
- 传**单个数字**：创建指定长度的稀疏数组（空槽位）
- 传**多个值**：直接作为数组元素
- 是否修改原数组：不涉及（创建操作）

```js
// 创建长度为10的空数组（每个位置都是空槽 empty）
var arr = new Array(10);
console.log(arr);       // [empty × 10]
console.log(arr.length);// 10

// 创建包含指定元素的数组
var arr = new Array(1, 2, 3, 4, 5);
console.log(arr);       // [1, 2, 3, 4, 5]
```

⚠️ 注意：`new Array(10)` 是**稀疏数组**，空位不是 `undefined`，`forEach` 等遍历方法会跳过空位。实际开发推荐用字面值创建数组。

### 2. 字面值方式（推荐）
用 `[]` 直接创建，最直观、最常用：
```js
// 直接创建带元素的数组
var arr = [1, 2, 3, 4, 5];
console.log(arr);       // [1, 2, 3, 4, 5]
console.log(arr.length);// 5

// 创建空数组
var emptyArr = [];
```

---

## 二、首尾增删方法（栈/队列操作）
### 1. push() 末尾添加  `/pʊʃ/`
- **作用**：在数组**末尾**添加一个或多个元素
- **语法**：`arr.push(元素1, 元素2...)`
- **返回值**：添加后数组的新长度
- **是否修改原数组**：✅ 是

```js
var fi = ["apple", "banana", "orange"];
var newLength = fi.push("pear");
console.log(fi);        // ["apple", "banana", "orange", "pear"]
console.log(newLength); // 4（数组新长度）
```

### 2. pop() 末尾删除
- **作用**：删除数组**最后一个**元素
- **语法**：`arr.pop()`
- **返回值**：被删除的那个元素
- **是否修改原数组**：✅ 是

#### 基础删除
```js
var fii = ["apple", "banana", "orange"];
fii.pop();
console.log(fii);       // ["apple", "banana"]
console.log(fii.length);// 2
```

#### 获取被删除的元素
```js
var food = ["煎饼", "油条", "豆浆"];
var GoodFood = food.pop();
console.log(GoodFood);  // "豆浆"（拿到被删掉的元素）
console.log(food);      // ["煎饼", "油条"]
```

#### push + pop 配对使用（栈结构：后进先出）
```js
let list = [];
list.push('a'); // 末尾加 'a' → ['a']
list.push('b'); // 末尾加 'b' → ['a', 'b']
list.pop();     // 末尾删最后一个 → ['a']
console.log(list); // ['a']
```

### 3. unshift() 开头添加
- **作用**：在数组**开头**添加一个或多个元素
- **语法**：`arr.unshift(元素1, 元素2...)`
- **返回值**：添加后数组的新长度
- **是否修改原数组**：✅ 是

```js
var num = [2, 3, 4];
num.unshift(0, 1);
console.log(num); // [0, 1, 2, 3, 4]
```

### 4. shift() 开头删除
- **作用**：删除数组**第一个**元素
- **语法**：`arr.shift()`
- **返回值**：被删除的那个元素
- **是否修改原数组**：✅ 是

```js
var f = ["apple", "banana", "orange"];
f.shift();
console.log(f);         // ["banana", "orange"]
console.log(f.length);  // 2
```

💡 首尾方法速查表
| 方法      | 操作位置 | 返回值         | 修改原数组 |
|-----------|----------|----------------|------------|
| push()    | 末尾添加 | 数组新长度     | ✅ 是      |
| pop()     | 末尾删除 | 被删除的元素   | ✅ 是      |
| unshift() | 开头添加 | 数组新长度     | ✅ 是      |
| shift()   | 开头删除 | 被删除的元素   | ✅ 是      |

---

## 三、翻转与排序
### 1. reverse() 翻转数组 `/rɪˈvɜːs/`
- **作用**：将数组元素顺序首尾颠倒
- **语法**：`arr.reverse()`
- **返回值**：翻转后的原数组
- **是否修改原数组**：✅ 是

```js
var str = ["a", "b", "c"];
str.reverse();
console.log(str); // ["c", "b", "a"]
```

### 2. sort() 数组排序  	`/sɔːt/`
- **作用**：对数组元素进行排序
- **语法**：`arr.sort(比较函数)`
  - 不传比较函数：默认将元素转为字符串，按 Unicode 编码顺序排序
  - 传比较函数：按自定义规则排序
- **返回值**：排序后的原数组
- **是否修改原数组**：✅ 是

#### （1）默认排序（仅适合纯字符串数组）
⚠️ **核心易错点**：默认排序**不是按数字大小**，是按字符串的 Unicode 码点比较。原示例中个位数排序看似正确属于巧合，多位数数字会直接排错。

```js
// 纯字符串数组：排序正常
var myArr1 = ['B', 'A', 'C'];
myArr1.sort();
console.log(myArr1); // ['A', 'B', 'C']

// 数字数组：默认排序错误！
var myArr = [3, 1, 10, 2];
myArr.sort();
console.log(myArr); // [1, 10, 2, 3]（按字符串比较，不是数字大小）
```

#### （2）数字升序（从小到大）
固定写法：传入比较函数 `(a, b) => a - b`
```js
let nums = [1, 10, 2, 20];
nums.sort((a, b) => a - b);
console.log(nums); // [1, 2, 10, 20]（正确的数字从小到大）
```

#### （3）数字降序（从大到小）
固定写法：传入比较函数 `(a, b) => b - a`
```js
let nums1 = [1, 10, 2, 20];
nums1.sort((a, b) => b - a);
console.log(nums1); // [20, 10, 2, 1]（正确的数字从大到小）
```

💡 比较函数原理：
- 返回负数 → `a` 排在 `b` 前面
- 返回正数 → `b` 排在 `a` 前面
- 返回 0 → 两者顺序不变

---

## 四、查找方法
### 1. indexOf() 从前向后查找
- **作用**：从数组头部开始，查找目标元素**第一次出现**的索引
- **语法**：`arr.indexOf(目标元素, 起始索引)`
  - 第二个参数可选：指定从哪个下标开始往后找，默认从 0 开始
- **返回值**：找到 → 元素下标；没找到 → `-1`
- **是否修改原数组**：❌ 否
- **匹配规则**：严格相等（`===`）

```js
var arr2 = [1, 2, 3, 4, 5];
console.log(arr2.indexOf(3)); // 2（找到，返回下标 2）
console.log(arr2.indexOf(6)); // -1（未找到，固定返回 -1）

// 指定起始位置查找
var arr = [1, 2, 3, 2, 1];
console.log(arr.indexOf(2, 2)); // 3（从下标 2 开始往后找）
```

### 2. lastIndexOf() 从后向前查找
- **作用**：从数组尾部开始，查找目标元素**最后一次出现**的索引
- **语法**：`arr.lastIndexOf(目标元素, 起始索引)`
- **返回值**：找到 → 元素下标；没找到 → `-1`
- **是否修改原数组**：❌ 否
- **匹配规则**：严格相等（`===`）

```js
var arr3 = [1, 2, 3, 4, 5, 3];
console.log(arr3.lastIndexOf(3)); // 5（最后一个 3 在下标 5）
console.log(arr3.lastIndexOf(6)); // -1（未找到）
```

### 3. includes() 判断是否包含  `/ɪnkˈluːdz/`
- **作用**：判断数组中是否包含指定元素，直接返回布尔值
- **语法**：`arr.includes(目标元素, 起始索引)`
- **返回值**：包含 → `true`；不包含 → `false`
- **是否修改原数组**：❌ 否

```js
var fruitss = ['apple', 'banana', 'orange'];
console.log(fruitss.includes('banana')); // true（包含）
console.log(fruitss.includes('pear'));   // false（不包含）
```

💡 indexOf vs includes 核心区别
1. 返回值不同：indexOf 返回下标，includes 返回布尔值
2. NaN 处理：indexOf 无法识别 `NaN`，includes 可以正确匹配
```js
let arr = [1, NaN, 3];
console.log(arr.indexOf(NaN));  // -1（找不到）
console.log(arr.includes(NaN)); // true（能正确匹配）
```
3. 场景选择：只判断「有没有」用 includes，更直观；需要知道「在第几位」用 indexOf

⚠️ 共同注意：两者都**不能直接按内容查找对象/数组**（引用类型比对内存地址，不是内容），查对象数组需要用 `findIndex()` / `find()` / `some()`。

---

## 五、转字符串与拼接
### 1. toString() 转逗号分隔字符串
- **作用**：将数组所有元素用英文逗号 `,` 拼接成字符串
- **语法**：`arr.toString()`
- **返回值**：拼接后的字符串
- **是否修改原数组**：❌ 否

```js
// 数字数组转字符串
let numss = [1, 2, 3, 4];
let str = numss.toString();
console.log(str); // "1,2,3,4"

// 字符串数组转字符串
let fruits = ['苹果', '香蕉', '橙子'];
console.log(fruits.toString()); // "苹果,香蕉,橙子"
```

### 2. join() 自定义分隔符拼接
- **作用**：将数组所有元素按指定分隔符拼接成字符串
- **语法**：`arr.join(分隔符)`
  - 不传分隔符：默认用英文逗号，和 `toString()` 效果一致
  - 传空字符串 `''`：元素无缝拼接
- **返回值**：拼接后的字符串
- **是否修改原数组**：❌ 否

```js
// 不传参数，默认逗号拼接
var arr = [1,2,3,4,5];
var str = arr.join();
console.log(str); // "1,2,3,4,5"

// 指定分隔符（最常用）
let date = [2026, 7, 6];
console.log(date.join('-'));  // "2026-7-6"
console.log(date.join('/'));  // "2026/7/6"
console.log(date.join(' '));  // "2026 7 6"

// 空字符串无缝拼接
let arr2 = ['a', 'b', 'c'];
console.log(arr2.join('')); // "abc"
```

💡 toString vs join 区别
- `toString()`：只能固定用逗号拼接，无自定义能力
- `join()`：可以自定义任意分隔符，更灵活，开发中优先使用

---

## 六、合并与截取
### 1. concat() 合并数组
- **作用**：合并两个或多个数组/元素，返回全新的数组
- **语法**：`arr.concat(值1, 值2...)`
  - 参数可以是数组，也可以是单个元素
- **返回值**：合并后的新数组
- **是否修改原数组**：❌ 否（不改动任何原数组）

#### 合并两个数组
```js
var arr1 = [1,2,3];
var arr2 = [4,5,6];
var arr3 = arr1.concat(arr2);
console.log(arr3); // [1, 2, 3, 4, 5, 6]
console.log(arr1); // [1,2,3]（原数组完全不变）
```

#### 同时合并数组和单个元素
```js
let arr = [1, 2];
let newArr = arr.concat(3, [4, 5], 6);
console.log(newArr); // [1, 2, 3, 4, 5, 6]
```

#### 复制数组（浅拷贝）
```js
let arr = [1, 2, 3];
let copyArr = arr.concat(); // 不传参数，复制整个数组
```

⚠️ 注意：concat 是浅拷贝，数组内的对象/数组只拷贝内存地址，修改新数组里的引用类型会影响原数组。

### 2. slice() 截取数组
- **作用**：截取数组中的一段元素，返回截取出来的新数组
- **语法**：`arr.slice(起始下标, 结束下标)`
  - 起始下标：开始截取的位置，默认 0；负数表示从末尾倒数
  - 结束下标：截取到该位置**之前**（左闭右开，不包含结束位置）；默认截到末尾；负数表示从末尾倒数
- **返回值**：截取出来的新子数组
- **是否修改原数组**：❌ 否（纯读取，不改动原数组）

#### 不传参数，复制整个数组（浅拷贝）
```js
let arr = [1, 2, 3];
let copyArr = arr.slice();

console.log(copyArr);          // [1, 2, 3]
console.log(arr === copyArr);  // false（两个不同的数组，内存地址不同）
```
> 为什么地址不同？数组是引用类型，`===` 比对的是内存地址。slice 会开辟新内存空间复制内容，是两个独立的数组，所以地址不同、返回 false。

#### 只传起始下标，截取到末尾
```js
let arr = ['a', 'b', 'c', 'd', 'e'];
let newArr = arr.slice(2); // 从下标 2 开始，截到最后
console.log(newArr); // ['c', 'd', 'e']
```

#### 传起止下标（左闭右开规则）
```js
let arr = ['a', 'b', 'c', 'd', 'e'];
let newArr = arr.slice(1, 3); // 取下标 1、2 的元素，不包含 3
console.log(newArr); // ['b', 'c']
```

#### 负数下标（从末尾倒数）
```js
let arr = ['a', 'b', 'c', 'd', 'e'];
console.log(arr.slice(-2));      // ['d', 'e']（取最后 2 个）
console.log(arr.slice(-3, -1));  // ['c', 'd']（倒数第3到倒数第1之前）
```

---

## 七、全能修改方法：splice()
- **作用**：可以同时实现**删除、添加、替换**数组元素，功能最全面
- **语法**：`arr.splice(起始下标, 删除数量, 新元素1, 新元素2...)`
  - 第 1 个参数：操作的起始位置
  - 第 2 个参数：要删除的元素个数；传 `0` 表示不删除，只添加
  - 第 3 个及以后：可选，要插入的新元素
- **返回值**：被删除元素组成的数组；没删除则返回空数组
- **是否修改原数组**：✅ 是（直接改动原数组，和 slice 完全相反）

### 1. 删除元素
#### 删除指定数量元素
```js
let arr = ['a', 'b', 'c', 'd'];
// 从下标 1 开始，删除 2 个元素
let deleted = arr.splice(1, 2);

console.log(arr);     // ['a', 'd'] 原数组被修改
console.log(deleted); // ['b', 'c'] 返回被删掉的元素组成的数组
```

#### 从指定位置删到末尾
```js
let arr = [1, 2, 3, 4, 5];
arr.splice(2); // 不传第二个参数，从下标 2 删到最后
console.log(arr); // [1, 2]
```

### 2. 添加元素（删除数量为 0）
```js
let arr = ['a', 'b', 'c'];
// 在下标 1 的位置，插入 'x' 和 'y'，不删除任何元素
arr.splice(1, 0, 'x', 'y');
console.log(arr); // ['a', 'x', 'y', 'b', 'c']
```

### 3. 替换元素（先删后加）
删除指定数量旧元素，同时在同位置插入新元素，实现替换效果
```js
let arr = ['a', 'b', 'c', 'd'];
// 从下标 1 开始，删 2 个旧元素，换成 2 个新元素
arr.splice(1, 2, 'x', 'y');

console.log(arr); // ['a', 'x', 'y', 'd']
```
> 执行过程拆解：
> 1. 定位到下标 1（元素 `'b'`）
> 2. 删除 2 个元素：删掉 `'b'`、`'c'`，数组变成 `['a', 'd']`
> 3. 在起始位置（下标 1）插入 `'x'`、`'y'`，最终变成 `['a', 'x', 'y', 'd']`

---

## 八、核心易错点总表
### 1. 是否修改原数组（必记）
| 修改原数组 | 不修改原数组 |
|------------|--------------|
| push       | indexOf      |
| pop        | lastIndexOf  |
| unshift    | includes     |
| shift      | toString     |
| reverse    | join         |
| sort       | concat       |
| splice     | slice        |

### 2. 高频坑点
1. **sort 默认排序**：数字排序必须写比较函数，不能直接用默认 `sort()`
2. **引用类型查找**：indexOf、includes 不能直接按内容查对象，只能比对内存地址
3. **slice vs splice 区分**：
   - slice：截取、不修改原数组、参数是「起止下标」
   - splice：剪接（删/增/改）、修改原数组、参数是「起始下标 + 删除数量」